pipeline {
    agent any

    tools {
        maven 'M3'
    }

    stages {
        stage('Maven Build') {
            steps {
                dir('app') {
                    sh 'pwd'
                    sh 'ls -l'
                    sh 'mvn clean package -DskipTests'
                }
            }
        }
    }

    post {
        success {
            echo '✅ Maven build successful'
        }
        failure {
            echo '❌ Maven build failed'
        }
    }
}

